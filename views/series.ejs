<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Series</title>
    <link rel="stylesheet" href="/public/css/style.css">
</head>
<body class="movies-page">
    <h1>Series</h1>
    <h2>For You:</h2>
    <div class="carousel-container1" id="list">

    </div>
</body>
</html>
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script>
$(document).ready(function () {
    function PageLoad() {
        $.ajax({
            url: '/users/series',
            type: 'GET',
            contentType: 'application/json',
            success: function (movies) {
                let list = "";
                console.log(movies)
                console.log('Task successfully added:', movies);
                for (var i = 0; i < movies.seriesList.length; i++) {         
                    list += `
                        <img src="https://${movies.seriesList[i].thumbnail}" class="thumbnail" alt="${movies.seriesList[i].movieID}">
                    `
                }
                document.getElementById('list').innerHTML = list
                },
                error: function (error) {
                    console.error('Error adding task:', error.responseText);
                }
            });
        }

        PageLoad();
      });   
</script>